// Generated by CoffeeScript 1.9.1
(function() {
  var Environment, Rule, env;

  Environment = require('models/environment');

  Rule = require('models/rule');

  env = new Environment({
    columns: 100,
    rows: 70,
    imgPath: "images/environments/field-72ppi.png",
    wrapEastWest: false,
    wrapNorthSouth: false,
    barriers: [[0, 340, 1000, 10], [330, 0, 10, 340], [660, 0, 10, 340]]
  });

  env.addRule(new Rule({
    test: function(agent) {
      return agent.get('has diabetes') !== true && agent.get('prone to diabetes') === 'prone' && agent.get('chow');
      return Math.random() < 0.03;
    },
    action: function(agent) {
      return agent.set('has diabetes', true);
    }
  }));

  require.register("environments/field", function(exports, require, module) {
    return module.exports = env;
  });

}).call(this);
